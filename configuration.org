* TODO
		SPEED UP EMACS
		https://news.ycombinator.com/item?id=27011013

		E-LISP
		- https://emacs.stackexchange.com/questions/47318/how-can-i-start-learning-emacs-lisp

		Investigate this pdf minor mode:  https://github.com/dalanicolai/pdf-continuous-scroll-mode.el
* Setup Melpa Package
#+BEGIN_SRC emacs-lisp
  (require 'package)
  (add-to-list 'package-archives
  		'("melpa" . "https://melpa.org/packages/"))
  (package-initialize)
#+END_SRC
* Packages
#+BEGIN_SRC emacs-lisp
	(setq evil-want-C-u-scroll t) ;; Ensure Ctrl-U scrolls up
	(unless (package-installed-p 'evil)
	  (package-install 'evil))
	(require 'evil)
	(evil-mode 1)

	(require 'evil-leader)
	(global-evil-leader-mode)

	(require 'projectile)
	(projectile-mode +1)
	(setq projectile-project-search-path '("~/Development/"))

	(require 'paren)
	(setq show-paren-style 'parenthesis)
	(show-paren-mode +1)

	(require 'org-bullets)
	(add-hook 'org-mode-hook #'org-bullets-mode)

	(ivy-mode 1)
	(setq ivy-use-virtual-buffers t)

	(require 'popwin)
	(popwin-mode 1)


	(require 'lsp-mode)
	(add-hook 'go-mode-hook #'lsp-deferred)

	(require 'orderless)
	(setq completion-styles '(orderless))
	(setq ivy-re-builders-alist '((t . orderless-ivy-re-builder)))
	;; Focus cursor in new split buffers
	(defadvice split-window (after move-point-to-new-window activate)
	"Moves the point to the newly created window after splitting."
	(other-window 1))

	;; Set up before-save hooks to format buffer and add/delete imports.
	;; Make sure you don't have other gofmt/goimports hooks enabled.
	(defun lsp-go-install-save-hooks ()
	(add-hook 'before-save-hook #'lsp-format-buffer t t)
	(add-hook 'before-save-hook #'lsp-organize-imports t t))
	(add-hook 'go-mode-hook #'lsp-go-install-save-hooks)

	;; Setup GOPATH for emacs
	(when (memq window-system '(mac ns))
	(exec-path-from-shell-initialize))
	(exec-path-from-shell-copy-env "GOPATH")

	;; Use Treemac Icons in Dired
	(treemacs-icons-dired-mode)

	(add-hook 'prog-mode-hook 'linum-mode) ;; only enable line numbers for text/source code

	(pdf-tools-install)

	(require 'ivy-rich)
	(ivy-rich-mode 1)
	(setcdr (assq t ivy-format-functions-alist) #'ivy-format-function-line)

	;;(require 'org-roam)
	;;(setq org-roam-directory "~/org-roam")
  ;;(setq org-roam-completion-system 'ivy)

#+END_SRC
* Autogenerated code
#+BEGIN_SRC emacs-lisp
		(custom-set-variables
		;; custom-set-variables was added by Custom.
		;; If you edit it by hand, you could mess it up, so be careful.
		;; Your init file should contain only one such instance.
		;; If there is more than one, they won't work right.
		'(package-selected-packages
		'(orderless treemacs-icons-dired lsp-ui evil-leader popwin treemacs-evil treemacs counsel tron-legacy-theme alect-themes org-bullets go-mode projectile lsp-mode org-roam org evil)))
		(custom-set-faces
		;; custom-set-faces was added by Custom.
		;; If you edit it by hand, you could mess it up, so be careful.
		;; Your init file should contain only one such instance.
		;; If there is more than one, they won't work right.
		)
#+END_SRC
* Set variables
#+BEGIN_SRC emacs-lisp
	(menu-bar-mode -1)
	(add-to-list 'default-frame-alist '(fullscreen . maximized))
	(setq inhibit-startup-message t)
	(setq initial-scratch-message "")
	(setq ring-bell-function 'ignore)
	(set-default 'truncate-lines t)  ;; Don't wrap lines
	(toggle-scroll-bar -1)  ;; Don't show scroll bars
	(global-font-lock-mode 1)  ;; enable syntax highlighting
	(setq inhibit-compacting-font-caches t)

	(setq scroll-step 1 scroll-conservatively  10000) ;; Smoother scrolling with smaller steps

	(setq split-width-threshold nil) ;; Perform splits horizontally
	(setq split-height-threshold 0)
	(setq-default tab-width 2)

	;; (setq org-roam-db-update-method 'immediate)
	;; (setq org-roam-dailies-directory "~/org-roam/daily")
	;; (setq org-roam-capture-templates)
#+END_SRC
* Color Theme
#+BEGIN_SRC emacs-lisp
  (load-theme 'tron-legacy t)
#+END_SRC
* Keybindings
#+BEGIN_SRC emacs-lisp
	(evil-leader/set-leader ",")  ;; Enable Evil-Leader Key
	(with-eval-after-load 'evil
		;; use evil mode in the buffer created from calling `list-packages'.
		(add-to-list 'evil-buffer-regexps '("*Packages*" . normal))

		(with-eval-after-load 'package
		;;(define-key evil-motion-state-map ":" nil)
		;;(define-key evil-normal-state-map ":" nil)
		(define-key evil-normal-state-map (kbd "C-n") nil)
		(define-key evil-normal-state-map (kbd "C-p") nil)

		(define-key evil-normal-state-map (kbd "C-h") 'evil-window-left)
		(define-key evil-normal-state-map (kbd "C-j") 'evil-window-down)
		(define-key evil-normal-state-map (kbd "C-k") 'evil-window-up)
		(define-key evil-normal-state-map (kbd "C-l") 'evil-window-right)

		(evil-define-key 'normal
		(evil-define-key 'normal package-menu-mode-map (kbd "m") #'package-menu-mark-install)
		(evil-define-key 'normal package-menu-mode-map (kbd "u") #'package-menu-mark-unmark)
		(evil-define-key 'normal package-menu-mode-map (kbd "x") #'package-menu-execute))))

		(define-key ivy-minibuffer-map (kbd "C-j") 'ivy-next-line)
		(define-key ivy-minibuffer-map (kbd "C-k") 'ivy-previous-line)


		;;bookmark-menu-mode-map
		(with-eval-after-load "bookmark"
				(define-key bookmark-bmenu-mode-map (kbd "C-h") 'evil-window-left)
				(define-key bookmark-bmenu-mode-map (kbd "C-j") 'evil-window-down)
				(define-key bookmark-bmenu-mode-map (kbd "C-k") 'evil-window-up)
				(define-key bookmark-bmenu-mode-map (kbd "C-l") 'evil-window-right)
				(define-key bookmark-bmenu-mode-map (kbd "j") 'evil-next-line)
				(define-key bookmark-bmenu-mode-map (kbd "k") 'evil-previous-line)
				(define-key bookmark-bmenu-mode-map (kbd "l") 'evil-forward-char)
				(define-key bookmark-bmenu-mode-map (kbd "h") 'evil-backward-char))

		;; Package mode
		(define-key package-menu-mode-map (kbd "C-h") 'evil-window-left)
		(define-key package-menu-mode-map (kbd "C-j") 'evil-window-down)
		(define-key package-menu-mode-map (kbd "C-k") 'evil-window-up)
		(define-key package-menu-mode-map (kbd "C-l") 'evil-window-right)

		(define-key pdf-view-mode-map "j" 'pdf-view-next-line-or-next-page)
		(define-key pdf-view-mode-map "k" 'pdf-view-previous-line-or-previous-page)

		;; My own minor-mode ensuring my keybindings persist
		(defvar my-keys-minor-mode-map
				(let ((map (make-sparse-keymap)))
				(evil-define-key 'normal 'global (kbd "C-z") 'list-bookmarks)
				(define-key map (kbd "C-w =") 'balance-windows)

				;; TODO Need to be able to press colon key in insert mode without
				;; it triggering `execute-extended-command

				;;(define-key map ":" 'execute-extended-command)
				(define-key map (kbd "C-p") 'projectile-find-file)
				(define-key map (kbd "C-n") 'treemacs)


				;; Evil leader based keybindings
				(evil-leader/set-key
						;; TODO need to create a way to invoke vim command mode ':' otherwise cannot perform %s/old/new/g substitution
						"p" 'projectile-switch-project
						"f" 'lsp-find-definition
						"q" 'delete-window
				)
				map)
		"my-keys-minor-mode keymap.")

						(define-minor-mode my-keys-minor-mode
								"A minor mode so that my key settings override annoying major modes."
								:init-value t
								:lighter " my-keys")

	(my-keys-minor-mode 1)

	;; (bind-key "C-h /" 'org-roam-find-file)
	;; (bind-key [f8] 'org-roam-find-file)
	;; (bind-key "C-h 0" 'rg-roam-capture)
	;; (bind-key "C-h i" 'org-roam-insert)
	;; (bind-key "C-h d" 'org-roam-find-directory)
	;; (bind-key "C-h u" 'org-roam-db-build-cache)
	;; (bind-key "C-h t" 'org-roam-dailies-capture-today)
	;; (bind-key "C-h j" 'org-roam-dailies-find-date)
	;; (bind-key "C-h y" 'org-roam-dailies-find-yesterday)
	;; (bind-key "C-h x" 'org-roam-switch-to-buffer)
	;; (bind-key "C-h r" 'org-roam-buffer-toggle-display)

	;; (setq org-roam-db-update-method 'immediate)
	;; (setq org-roam-dailies-directory "~/org-roam/daily")

#+END_SRC
